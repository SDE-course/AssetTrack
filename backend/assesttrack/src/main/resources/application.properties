server.port=8080

# ===================================
# PostgreSQL - Neon DB
# ===================================
spring.datasource.url=jdbc:postgresql://ep-ancient-cloud-apqixqmj-pooler.c-7.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_TZUXQbfvkl71
spring.datasource.driver-class-name=org.postgresql.Driver

# Connection Pool
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.keepalive-time=300000

# ===================================
# JPA / Hibernate
# ===================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ===================================
# JWT
# ===================================
jwt.secret=CHANGE_ME
jwt.expiration=86400000
app.jwt.secret=

# ===================================
# Logging
# ===================================
logging.level.com.assettrack=DEBUG
logging.level.org.springframework.security=DEBUG

# ===================================
# Email — Mailtrap SMTP
# ===================================
# Sign up free at https://mailtrap.io → Email Testing → SMTP Settings
# Copy the credentials from the "Spring Boot" tab and paste them below
 
spring.mail.host=sandbox.smtp.mailtrap.io
spring.mail.port=2525
spring.mail.username=095553bdcdc9ca
spring.mail.password=f3167b3e43f702
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

notifications.email.enabled=true
 
# The "from" address shown in the email (can be anything for Mailtrap testing)
notifications.email.from=noreply@assettrack.com
 
# The inbox that receives all alerts — use your own email or the Mailtrap test address
notifications.email.to=shahd.ibrahim05@eng-st.cu.edu.eg

# ===================================
# Notification Settings
# ===================================

# How many days before expiry to fire a warranty alert
notifications.warranty-warning-days=30

# Available-stock count at or below which a low-stock alert fires
notifications.low-stock-threshold=3

# Asset types to monitor for low stock (comma-separated, must match your AssetType enum names)
notifications.low-stock-types=LAPTOP, DESKTOP, MONITOR, KEYBOARD, MOUSE, PRINTER, TABLET, MOBILE, NETWORK_DEVICE